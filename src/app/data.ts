export interface getData {
    productName: string,
    productId: string,
    availableQuantity: number,
}
export interface loadData {
    orderId: string,
    customerId: string,
    productId: string,
    quantity: number,
}